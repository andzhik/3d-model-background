# Implementation progress

## Prompt 01 — Scaffold a working application

- **Completion date:** 2026-07-14
- **Outcome:** Created a Vite React TypeScript application with a full-viewport semantic hero, visible headline, subtitle, call to action, and the supplied `images/yoge-lemur.png` artwork as its static background.
- **Decisions:** Kept the hero content in accessible HTML, separate from the future decorative canvas. Used a CSS asset reference so Vite bundles the existing image without modifying or duplicating the source file. Selected centered upper-sky content to match the design-plan default.
- **Tooling:** Added React Three Fiber, Three.js, Drei, Vitest, Testing Library, ESLint, and Prettier, with development, build, lint, typecheck, test, and formatting scripts.
- **Verification:** `npm install` completed with 287 audited packages and no reported vulnerabilities. `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, and the Prettier check all passed. Vitest ran 1 test file with 1 passing test. Vite emitted `dist/index.html`, the bundled poster image, CSS, and JavaScript successfully; the JavaScript bundle measured 191.11 kB (60.25 kB gzip). `npm run dev` started successfully and remained available until the verification process was stopped.
- **Remaining limitations:** This step intentionally provides only the static poster baseline. Responsive focal-point tuning and the WebGL canvas are reserved for later prompts.

## Prompt 02 — Establish responsive static composition

- **Completion date:** 2026-07-14
- **Outcome:** Upgraded the static poster hero with an explicit responsive composition system, an upper-sky contrast scrim, constrained content sizing, and a semantic no-JavaScript fallback.
- **Responsive focal-point decisions:** The 3:2 source keeps the lemur on its centered 50% horizontal axis at every target size. Desktop landscape uses `50% 50%` to preserve both mountain walls and the sun/reflection corridor. Tablet portrait retains that center while accepting intentional cropping of the outer trees and mountains. Mobile portrait uses `50% 48%`, keeping the lemur's face and the slightly right-of-center sun inside the narrow crop while reserving the upper-center sky for copy.
- **CSS configuration:** Added `--hero-height`, `--hero-content-width`, `--hero-scrim-intensity`, and `--hero-focal-position`. Breakpoint overrides cover desktop, tablet portrait, and mobile portrait without changing the semantic content.
- **Contrast decision:** Layered a restrained top-weighted linear scrim with a localized radial scrim behind the approved upper-center content zone. This darkens the bright sky enough for white copy while leaving the mountains, water, and character artwork substantially unchanged.
- **Verification:** `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, and `npm run format:check` passed. Vitest ran 1 test file with 2 passing semantic-structure tests. Vite emitted the production build successfully: 1.18 kB HTML (0.56 kB gzip), 2.91 kB CSS (1.29 kB gzip), 191.11 kB JavaScript (60.25 kB gzip), and the unchanged 2,058.75 kB poster asset. Browser inspection was attempted, but the available in-app browser runtime failed during initialization (`Cannot redefine property: process`), so no viewport screenshots are claimed or stored.
- **Remaining limitations:** This is intentionally a static poster composition. WebGL rendering, runtime camera presets, and animated-scene responsive rules remain reserved for later prompts. Automated visual baselines are also pending a working browser runtime.

## Prompt 03 — Add the WebGL shell and safe fallback

- **Completion date:** 2026-07-14
- **Outcome:** Added a decorative React Three Fiber canvas between the permanent poster and semantic hero content. The new `SceneBackground` owns renderer lifecycle and fallback state, while `Experience` supplies a perspective camera configuration, hemisphere and ambient fill, a warm directional light, and a visibly faceted icosahedron placeholder.
- **Loading and fallback decisions:** The canvas begins fully transparent and reports readiness only after a frame with a live WebGL context and finite camera matrix has rendered. Readiness is deferred to the following animation frame so the browser can paint the completed scene before the 600 ms fade begins. WebGL capability failure, React render errors, and `webglcontextlost` all move the layer to a terminal hidden fallback state. The poster remains the hero's CSS background in loading, success, and failure states, preventing a blank startup frame.
- **Accessibility:** Both the scene wrapper and renderer are `aria-hidden`; the entire scene layer has pointer events disabled. The title, supporting copy, and CTA remain ordinary semantic HTML above the canvas and contrast scrim.
- **Automated protection:** Added reducer tests for first-frame readiness, failures from every lifecycle state, and a late readiness callback after failure. Added a component test confirming that an environment without WebGL retains the fallback state and mounts no canvas.
- **Verification:** `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, and `npm run format:check` passed. Vitest ran 2 test files with 8 passing tests. Vite emitted the production build successfully: 1.18 kB HTML (0.56 kB gzip), 3.29 kB CSS (1.39 kB gzip), 1,075.36 kB JavaScript (295.33 kB gzip), and the unchanged 2,058.75 kB poster asset.
- **Remaining limitations:** The faceted object and lighting are intentionally temporary and will be replaced by later scene prompts. Vite reports that the initial JavaScript chunk exceeds 500 kB; code splitting and measured optimization remain future work once the scene architecture stabilizes. A live browser smoke check was attempted, but the available in-app browser runtime failed during initialization (`Cannot redefine property: process`), so no successful WebGL screenshot or manual visual result is claimed for this step.

## Prompt 04 — Create deterministic scene and Blender foundations

- **Completion date:** 2026-07-14
- **Outcome:** Centralized the scene camera, fog, lighting, placeholder geometry, renderer quality, and color roles into typed scene modules. Added a Mulberry32 seeded-random utility, shared flat-shaded material helpers, and explicit scene-quality types. Replaced the Prompt 03 scene's scattered visual values with named configuration.
- **Development controls:** Added a code-native development-only panel for camera position, field of view, fog near/far, key-light intensity, and major scene-group visibility. The panel is guarded by Vite's compile-time `import.meta.env.DEV` flag. A production bundle text search found no `Scene debug` UI marker, confirming the panel component was removed from production output.
- **Blender pipeline:** Added the `blender/scripts/`, `blender/generated/`, and `public/models/` conventions, a JSON asset manifest, environment-first Blender executable resolution, headless build orchestration, and GLB structural validation. The deterministic smoke-test generator clears Blender state, creates the flat-shaded `PipelineTestMesh` with `PipelineTestMaterial`, and exports `public/models/pipeline-test.glb`; `npm run assets:build:source` optionally retains the generated `.blend` for inspection.
- **Determinism and validation:** Blender 4.3.1 at the documented detected location produced a valid 9,400-byte GLB. Two consecutive headless builds had the identical SHA-256 digest `194463226F33A04D4433E71AFEF6849192E2B2DA16B820F2F989A90DC4B6A5ED`. Validation confirmed the GLB container length, JSON chunk, expected object name, and expected material name. Unit tests prove same-seed sequence equality and requested decimal/integer range bounds.
- **Verification:** `npm run format:check`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run assets:build`, and `npm run build` passed. Vitest ran 3 files with 11 passing tests. The production build emitted 1.18 kB HTML (0.56 kB gzip), 4.17 kB CSS (1.67 kB gzip), 1,078.30 kB JavaScript (296.44 kB gzip), and the unchanged 2,058.75 kB poster asset.
- **Remaining limitations:** The debug visibility model anticipates the full scene graph, but Prompt 04 still has only the foreground placeholder and lighting/environment groups; distant and middle groups gain visible content in later prompts. Vite's existing JavaScript chunk-size warning remains and is reserved for the planned measured optimization work.

## Prompt 05 — Build the sky, sun, and clouds

- **Completion date:** 2026-07-14
- **Outcome:** Replaced the faceted placeholder with a procedural atmospheric scene containing a three-stop low-poly-reference sky gradient, an unlit warm sun disc near the horizon, and 12 polygonal clouds split across far and near depth layers. The supplied poster remains permanently visible beneath the translucent WebGL layer while later environment features are incomplete.
- **Composition decisions:** Kept the sun at world `x = 0` so the centered perspective camera projects it onto the central vertical axis at every target aspect ratio. Placed it below camera center at `y = -1.45` to establish the future valley horizon. The oversized sky plane covers the widest target frustum without relying on responsive camera changes reserved for Prompt 14.
- **Determinism and motion:** Cloud counts, positions, scales, and one of three faceted silhouettes come from the existing seeded random generator and named atmosphere configuration. Far clouds sit at `z = -10` and drift at `0.018` world units per second; near clouds sit at `z = -5` and drift at `0.038` world units per second. Positions are calculated from elapsed seconds rather than accumulated frame deltas, then wrapped into a fixed field. A `useSyncExternalStore` reduced-motion hook tracks `prefers-reduced-motion` and freezes both layers at their deterministic initial positions.
- **Automated protection:** Added tests for repeatable placement, configured range bounds, elapsed-time drift and wrapping, and the near layer's greater depth and speed. The suite now contains 4 test files with 15 passing tests.
- **Verification:** `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, and `npm run format:check` passed. Vite emitted 1.18 kB HTML (0.56 kB gzip), 4.17 kB CSS (1.67 kB gzip), 1,082.23 kB JavaScript (297.89 kB gzip), and the unchanged 2,058.75 kB poster asset. Browser viewport inspection was attempted, but the available in-app browser again failed during initialization (`Cannot redefine property: process`), so no rendered screenshot inspection is claimed. Central sun alignment is guaranteed by the fixed zero X position and centered camera projection, and is covered across aspect ratios by the camera/plane geometry.
- **Remaining limitations:** Mountains, lake, shore, and character geometry are intentionally absent, so the poster remains visually important beneath the procedural atmosphere. Live viewport screenshots remain pending a working browser runtime. The existing Vite large-chunk warning remains reserved for later measured optimization prompts.

## Prompt 06 — Generate layered low-poly mountains

- **Completion date:** 2026-07-14
- **Outcome:** Added a deterministic Blender mountain generator and exported `public/models/environment-mountains.glb`. The asset contains flat-shaded triangular `MountainLayerFar`, `MountainLayerLeft`, and `MountainLayerRight` meshes with a compact cool blue-violet and peach-lit four-material palette. The authored silhouettes deliberately form a dominant left peak, tall right wall, low center horizon, and clear valley around the sun rather than a generic random range.
- **Runtime integration:** Added a typed Drei loader that checks required nodes, clones each layer, and mounts it beneath separately named far, left, and right web groups for later parallax. Asset loading now participates in the existing first-valid-frame gate. Three.js fog begins at 7.5 world units and reaches full haze at 24, separating the near walls from the far ridge while keeping the poster permanently available underneath.
- **Geometry and composition safeguards:** The generator uses fixed silhouette control points and seeded depth variation only for internal facets. It verifies finite vertices, a complete connected triangle grid, and front-facing polygon orientation before export. The three profiles span well beyond the current camera frustum, while their inward edges meet the low center horizon without covering the sun corridor.
- **Asset validation:** Extended the manifest and validator with per-asset minimum/maximum size limits, required mountain node/material names, and finite ordered bounds on every position accessor. `npm run assets:build` generated a 27,420-byte GLB twice; the repeat matched byte-for-byte with SHA-256 `30cc3e4d4f44d548f20345daa7c7344abea53dc303e5f58bd25097cf8d3783df`.
- **Verification:** `npm run assets:build`, `npm run assets:validate`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run format:check`, and `npm run build` passed. Vitest ran 4 files with 15 passing tests. Vite emitted 1.18 kB HTML (0.56 kB gzip), 4.17 kB CSS (1.67 kB gzip), 1,154.18 kB JavaScript (318.88 kB gzip), the 27.42 kB mountain GLB, and the unchanged 2,058.75 kB poster asset.
- **Remaining limitations:** The browser runtime again failed during initialization (`Cannot redefine property: process`), so no live viewport screenshots or visual inspection are claimed. The intended framing is protected structurally by explicit profiles, camera-relative coverage, winding checks, and GLB bounds validation, but final responsive camera tuning remains Prompt 14. The existing Vite large-chunk warning remains reserved for later measured optimization work; lake, shores, trees, and character geometry remain intentionally out of scope.

## Prompt 07 — Add the lake and stylized sunlight reflection

- **Completion date:** 2026-07-15
- **Outcome:** Added a subdivided lake plane extending from behind the camera to the mountain horizon. A lightweight custom vertex shader produces slow angular ripple displacement, while the fragment shader grades the surface from saturated cyan in the foreground to a cooler muted horizon blue with restrained bright facets.
- **Reflection implementation:** Added a separately named transparent `SunReflection` mesh on the lake surface. Its shader narrows the centered highlight toward the sun and breaks the path into moving horizontal fragments. It samples no textures and renders no reflected scene, so the effect adds one ordinary draw call rather than a planar-reflection pass.
- **Configuration and motion:** Centralized water `speed` (`0.22` shader-time units per second), `amplitude` (`0.055` world units), and `reflectionWidth` (`2.8`) in `WATER_CONFIG`, together with lake dimensions and subdivision counts. Both water shaders derive animation directly from elapsed time. The existing reduced-motion preference freezes their time uniforms at zero, retaining a deterministic static lake and reflection.
- **Automated protection:** Added unit coverage for configured time scaling, reduced-motion freezing, calm amplitude bounds, named reflection width, shared displacement uniforms, and the reflection shader's lack of a sampler-based render dependency.
- **Verification:** `npm run format:check`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed. Vitest ran 5 files with 18 passing tests. Vite emitted 1.18 kB HTML (0.56 kB gzip), 4.17 kB CSS (1.67 kB gzip), 1,157.62 kB JavaScript (319.80 kB gzip), the existing 27.42 kB mountain GLB, and the unchanged 2,058.75 kB poster asset.
- **Remaining limitations:** A development browser smoke check was attempted specifically to inspect WebGL shader compilation, but the available in-app browser failed during initialization (`Cannot redefine property: process`) before opening the local page. Therefore no live shader-console result or visual motion inspection is claimed. The GLSL sources are production-bundled and their timing/configuration paths are typechecked and unit-tested; a live GPU compilation check remains pending a working browser runtime. The existing Vite large-chunk warning remains reserved for later measured optimization prompts.

## Prompt 08 — Create shores, rocks, and foreground framing

- **Completion date:** 2026-07-15
- **Outcome:** Added a deterministic scripted-Blender foreground asset containing named left/right shore meshes, three reusable small deformed-icosahedron rock sources, two larger asymmetric corner-framing rock sources, and a centered twelve-sided meditation platform. All meshes are flat shaded and use a controlled six-material shore/rock/platform palette with deterministic face variation.
- **Runtime integration:** Added reusable `RockAsset` and `RockField` components. The two large corner rocks use the shared asset component, while 18 seeded small-rock placements are split across only three `InstancedMesh` groups by source variant. Shores belong to the middle-world visibility group; platform and rocks belong to the independent foreground group reserved for stronger future parallax.
- **Composition safeguards:** Repeated placements are constrained to both outside edges with an explicit `2.65` world-unit protected half-width around the future lemur. Their nearest Z position is capped below `1.5`, leaving more than three world units of clearance from the camera at Z `5`. Large rocks frame `x = -5.4` and `x = 5.3`, while the central platform alone establishes the character position and the upper-page control zone remains untouched.
- **Asset generation and validation:** Extended the headless manifest with all eight required node names, six required material names, finite ordered position-accessor bounds, a 10–250 kB size envelope, and deterministic repeat generation. Blender 4.3.1 generated a 32,372-byte `public/models/environment-foreground.glb`; the repeat matched byte-for-byte with SHA-256 `d9a4ab8e434be25896113779549f2d5f7f2b3366f815987534237d36433c04a5`.
- **Automated protection:** Added tests for deterministic placement, the configured total of 18 small rocks, both-side framing, protected character corridor, and camera clearance. The suite now contains 6 test files with 21 passing tests.
- **Verification:** `npm run assets:build`, `npm run assets:validate`, `npm run format:check`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed. Vite emitted 1.18 kB HTML (0.56 kB gzip), 4.84 kB CSS (1.82 kB gzip), 1,161.22 kB JavaScript (321.04 kB gzip), the 32.37 kB foreground GLB, the existing mountain GLB, and the unchanged 2,058.75 kB poster asset.
- **Remaining limitations:** A local rendered smoke check was attempted through the available browser-control skill, but browser initialization failed with `Cannot redefine property: process`, so no live visual inspection is claimed. Foreground depth and framing are protected structurally through separate scene groups, explicit near/side placement bounds, and deterministic tests; final responsive visibility tuning remains Prompt 14. The existing Vite large-chunk warning remains reserved for measured optimization work.

## Prompt 09 — Add instanced pine forests and plants

- **Completion date:** 2026-07-15
- **Outcome:** Added three procedural low-poly pine variants (`spire`, `compact`, and `asymmetric`) assembled from faceted stacked cones and simple trunks. Each variant is merged into one vertex-colored geometry and rendered through `InstancedMesh` batches in separate far and near forest bands. Added a shared three-blade grass/reed geometry rendered as one foreground instanced batch.
- **Composition decisions:** Far trees use a cooler, lighter haze palette and smaller scale range; near trees use a darker desaturated green and larger scale range to create strong forest-edge framing. Deterministic left/right placement bounds reserve a `3.05` world-unit central lake corridor, including conservative canopy and reed extents rather than only trunk origins. Sparse reeds remain outside the same protected corridor.
- **Quality and batching:** Exposed vegetation counts on every scene quality tier. High renders 72 far trees, 42 near trees, and 54 reed tufts (168 instances); medium renders 48, 28, and 36 (112 instances); low renders 24, 14, and 18 (56 instances); poster-only renders none. Three variants across two bands plus the shared reed batch produce seven expected vegetation draw calls on rendered tiers instead of one call per tree or tuft.
- **Development diagnostics:** Extended the development-only scene panel with high/medium/low selection, deterministic expected vegetation instance/draw-call counts, and sampled live renderer draw calls and triangles. The diagnostics remain excluded behind the existing development build guard.
- **Automated protection:** Added tests for deterministic placement at every rendered tier, exact configured counts, conservative open-corridor bounds, poster-only zero work, seven expected instanced draw calls, and the visibly lower low-quality density. The suite now contains 7 test files with 29 passing tests.
- **Verification:** `npm run typecheck`, `npm run lint`, `npm test`, a Prettier check of the Prompt 09 files, and `npm run build` passed. Vite emitted 1.18 kB HTML (0.56 kB gzip), 5.12 kB CSS (1.87 kB gzip), 1,170.13 kB JavaScript (323.68 kB gzip), the existing GLB assets, and the unchanged 2,058.75 kB poster image. A production bundle text check found no development diagnostics labels.
- **Remaining limitations:** The local in-app browser again failed during runtime initialization with `Cannot redefine property: process`, before opening the development page, so no live renderer-stat or visual-composition inspection is claimed. Expected batching and composition constraints are verified structurally and by tests, while a live GPU measurement remains pending a working browser runtime. Responsive vegetation hiding and automatic quality selection remain reserved for Prompts 14 and 16. The existing Vite large-chunk warning remains reserved for measured optimization work.

## Prompt 10 — Generate the lemur blockout in Blender

- **Completion date:** 2026-07-15
- **Outcome:** Added a modular deterministic Blender generator for a centered seated lemur blockout and exported `public/models/lemur.glb`. The figure uses flat-shaded low-resolution icospheres, tapered seven-sided cylinders, wedge/custom ear meshes, and a compact four-material charcoal/gray/light/amber palette. Its front silhouette includes a large faceted head, muzzle, ears, crossed legs, centered meditation hands, and an upright blockout tail.
- **Animation-ready organization:** Built a single `Root` hierarchy with named pelvis, torso, chest, neck, head, ear, muzzle, eye, paired upper/lower arm, hand, upper/lower leg, foot, and tail-root transform nodes. Mesh objects are children of their corresponding transform nodes so later animation work can move stable pivots without remodeling the blockout.
- **Runtime integration:** Added a strict typed `Lemur` loader and a shared asset-contract module. The loader validates all 23 required transform nodes before cloning `Root`, then mounts the character in the foreground scene group. Unit tests cover complete and incomplete hierarchy branches.
- **Preview and visual inspection:** The generator creates deterministic 640×640 front and three-quarter PNGs under `blender/generated/previews/`. Both were visually inspected after generation: the front pose reads symmetrically with centered hands and crossed legs, while the three-quarter view preserves muzzle, seated posture, and tail separation. The preview PNGs measure 354,882 and 351,892 bytes. A live desktop/mobile web inspection was attempted through the browser-control skill, but its runtime failed during initialization with `Cannot redefine property: process`; no live target-viewport result is claimed. Final responsive camera framing remains scheduled for Prompt 14.
- **Asset validation and determinism:** Extended the manifest/build pipeline to pass preview destinations, verify both non-empty PNG signatures, require every hierarchy/material name, check finite ordered position bounds, enforce a 20–500 kB GLB envelope, and perform byte-for-byte repeat builds. Blender 4.3.1 generated a valid 78,260-byte GLB; the repeat matched SHA-256 `d1f420a012fd18027c6ed538ae7c1faca11b2f65c2e8305acffa272a11de1920`.
- **Verification:** `npm run assets:build`, `npm run assets:validate`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed. Vitest ran 8 files with 31 passing tests. Vite emitted 1.18 kB HTML (0.55 kB gzip), 5.12 kB CSS (1.87 kB gzip), 1,170.78 kB JavaScript (323.89 kB gzip), the 78.26 kB lemur GLB, existing environment assets, and the unchanged poster. The final repository-wide formatting check passed after mechanical formatting.
- **Remaining limitations:** Facial mask regions, refined meditation fingers, crossed-leg anatomy, and the curved striped ring tail are deliberately left for Prompt 11. The current tail is an unstriped four-segment silhouette blockout. The existing Vite large-chunk warning remains reserved for measured optimization work.
